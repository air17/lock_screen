<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        BIND_ACCESSIBILITY_SERVICE is granted automatically to declared
        accessibility services – no runtime request needed.
        No other dangerous permissions are required.
    -->

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!--
            Transparent, no-UI activity launched by the app icon.
            It either locks the screen immediately (if the accessibility
            service is already enabled) or guides the user to Settings.
        -->
        <activity
            android:name=".LockActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Accessibility service that calls GLOBAL_ACTION_LOCK_SCREEN.
            The system binds this service; it does NOT need to be started
            by the app. BIND_ACCESSIBILITY_SERVICE protects the binding.
        -->
        <service
            android:name=".LockScreenService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
